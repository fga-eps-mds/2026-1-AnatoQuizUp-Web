import {
  PasswordRecoveryLayout,
  ResetPasswordForm,
} from '../../../features/recover-password';

export const ResetPasswordPage = () => (
  <PasswordRecoveryLayout>
    <ResetPasswordForm />
  </PasswordRecoveryLayout>
);
